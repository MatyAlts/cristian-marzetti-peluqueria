services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ""
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - marzetti

networks:
  marzetti:
    external: true
    name: marzetti-network
